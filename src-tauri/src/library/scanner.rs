#![allow(dead_code)]
//! Recursive directory scanner for music files
//! Runs on background thread, emits progress events

pub struct Scanner {}

impl Scanner {
    pub fn new() -> Self {
        Self {}
    }
}

# MusicPlayer v0.8.0 质量加固 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 v0.7.0 基础上，通过测试补齐、E2E Spike、性能优化、可访问性修复，将 MusicPlayer 提升到可发布质量。

**Architecture:** 不改动 Rust 音频内核和数据库模型。前端补充单元/组件测试覆盖关键模块，引入 Playwright E2E（或降级 Vitest 集成测试），TrackTable 虚拟滚动优化大列表，可访问性修复 AA 阻塞项。

**Tech Stack:** Vitest, Testing Library, userEvent, Playwright (A 档) 或 Vitest 集成测试 (B 档), @tanstack/react-virtual, focus-trap-react, Tauri 2, React 19, TypeScript strict.

---

**Phase 依赖：**
- Phase 0 → Phase 1 + Phase 2（可并行）
- Phase 1 → Phase 3（测试保护）
- Phase 1-3 → Phase 4（回归门禁）

---

### Task 1: Phase 0 — 质量基线记录

**Files:**
- Create: `docs/superpowers/plans/2026-05-02-v0.8.0-baseline.md`

- [ ] **Step 1: 记录前端测试基线**

```bash
npx vitest run 2>&1 | tee /tmp/v080-baseline-tests.txt
```

记录：测试文件数、测试通过数、测试失败数。

- [ ] **Step 2: 记录 bundle 大小**

```bash
pnpm build 2>&1 | tee /tmp/v080-baseline-build.txt
```

记录：JS 体积、CSS 体积。

- [ ] **Step 3: 记录机器信息**

```bash
sw_vers && node --version && rustc --version
```

记录：macOS 版本、Node 版本、Rust 版本。

- [ ] **Step 4: 写入基线文件**

创建 `docs/superpowers/plans/2026-05-02-v0.8.0-baseline.md`：

```markdown
# v0.8.0 质量基线

记录时间：2026-05-02
机器：（填写型号）
macOS：（填写版本）
Node：（填写版本）
Rust：（填写版本）

## 前端测试
- 测试文件数：21
- 测试通过数：43
- 测试失败数：0

## Bundle 大小
- JS：（填写）
- CSS：（填写）

## 首屏时间
- （待 `pnpm tauri build --debug` 后测量）

## TrackTable DOM 行数
- （待 1000 曲目场景后测量）

## Lighthouse A11y 分数
- （待测量）
```

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/plans/2026-05-02-v0.8.0-baseline.md
git commit -m "docs: record v0.8.0 quality baseline"
```

---

### Task 2: Phase 1 — useDebouncedValue 测试

**Files:**
- Create: `src/hooks/useDebouncedValue.test.ts`

- [ ] **Step 1: 写失败测试**

```ts
import { describe, expect, it, vi, beforeEach, afterEach } from "vitest";
import { renderHook, act } from "@testing-library/react";
import { useDebouncedValue } from "./useDebouncedValue";

describe("useDebouncedValue", () => {
  beforeEach(() => {
    vi.useFakeTimers();
  });
  afterEach(() => {
    vi.useRealTimers();
  });

  it("returns initial value immediately", () => {
    const { result } = renderHook(() => useDebouncedValue("hello", 250));
    expect(result.current).toBe("hello");
  });

  it("debounces value changes", () => {
    const { result, rerender } = renderHook(
      ({ value }) => useDebouncedValue(value, 250),
      { initialProps: { value: "initial" } },
    );
    rerender({ value: "updated" });
    expect(result.current).toBe("initial");
    act(() => {
      vi.advanceTimersByTime(250);
    });
    expect(result.current).toBe("updated");
  });

  it("cancels previous timer on rapid changes", () => {
    const { result, rerender } = renderHook(
      ({ value }) => useDebouncedValue(value, 250),
      { initialProps: { value: "a" } },
    );
    rerender({ value: "b" });
    act(() => {
      vi.advanceTimersByTime(100);
    });
    rerender({ value: "c" });
    act(() => {
      vi.advanceTimersByTime(250);
    });
    expect(result.current).toBe("c");
  });
});
```

- [ ] **Step 2: 运行测试确认通过**

```bash
npx vitest run src/hooks/useDebouncedValue.test.ts --reporter=verbose
```

Expected: 3 tests PASS（hook 已实现）。

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useDebouncedValue.test.ts
git commit -m "test: add useDebouncedValue tests"
```

---

### Task 3: Phase 1 — uiStore 测试补充

**Files:**
- Modify: `src/stores/uiStore.test.ts`

- [ ] **Step 1: 在现有测试文件末尾追加测试**

```ts
it("toggleNowPlaying cycles correctly", () => {
  const store = useUIStore.getState();
  store.toggleNowPlaying();
  expect(useUIStore.getState().isNowPlayingOpen).toBe(true);
  store.toggleNowPlaying();
  expect(useUIStore.getState().isNowPlayingOpen).toBe(false);
  store.toggleNowPlaying();
  expect(useUIStore.getState().isNowPlayingOpen).toBe(true);
});

it("setResolvedTheme updates document dataset", () => {
  useUIStore.getState().setResolvedTheme("dark");
  expect(useUIStore.getState().resolvedTheme).toBe("dark");
  useUIStore.getState().setResolvedTheme("light");
  expect(useUIStore.getState().resolvedTheme).toBe("light");
});

it("initial state is correct", () => {
  expect(useUIStore.getState().isNowPlayingOpen).toBe(false);
  expect(useUIStore.getState().sidebarCollapsed).toBe(false);
  expect(useUIStore.getState().theme).toBe("system");
  expect(useUIStore.getState().resolvedTheme).toBe("light");
});
```

- [ ] **Step 2: 运行测试确认通过**

```bash
npx vitest run src/stores/uiStore.test.ts --reporter=verbose
```

Expected: 6 tests PASS。

- [ ] **Step 3: Commit**

```bash
git add src/stores/uiStore.test.ts
git commit -m "test: add uiStore toggle and initial state tests"
```

---

### Task 4: Phase 1 — useToggleFavoriteMutation 测试

**Files:**
- Create: `src/hooks/useToggleFavoriteMutation.test.ts`

- [ ] **Step 1: 写测试**

```ts
import { describe, expect, it, vi, beforeEach } from "vitest";
import { renderHook, act, waitFor } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import type { ReactNode } from "react";
import type { Track } from "@/repositories/trackRepo";

const setFavorite = vi.fn().mockResolvedValue(undefined);
vi.mock("@/repositories/favoriteRepo", () => ({
  favoriteRepo: { set: setFavorite },
}));

vi.mock("@tauri-apps/api/core", () => ({ invoke: vi.fn() }));
vi.mock("@tauri-apps/api/event", () => ({
  listen: vi.fn().mockResolvedValue(() => {}),
}));

const baseTrack: Track = {
  id: 1,
  filePath: "/a.mp3",
  fileSize: 1,
  fileModifiedAt: 1,
  hash: null,
  title: "Song A",
  albumId: null,
  primaryArtistId: null,
  albumArtistId: null,
  trackNo: null,
  discNo: null,
  year: null,
  genre: null,
  durationMs: 1000,
  bitrate: null,
  sampleRate: null,
  channels: null,
  codec: null,
  isFavorite: false,
  playCount: 0,
  lastPlayedAt: null,
  lastSeenAt: 1,
  missingAt: null,
  addedAt: 1,
  updatedAt: 1,
  rootFolderId: null,
  albumName: null,
  primaryArtistName: null,
};

describe("useToggleFavoriteMutation", () => {
  let qc: QueryClient;
  let wrapper: ({ children }: { children: ReactNode }) => JSX.Element;

  beforeEach(() => {
    setFavorite.mockClear();
    qc = new QueryClient({
      defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
    });
    wrapper = ({ children }: { children: ReactNode }) => (
      <QueryClientProvider client={qc}>{children}</QueryClientProvider>
    );
  });

  it("calls favoriteRepo.set with correct args", async () => {
    const { useToggleFavoriteMutation } = await import(
      "./useToggleFavoriteMutation"
    );
    const { result } = renderHook(() => useToggleFavoriteMutation(), {
      wrapper,
    });
    await act(async () => {
      result.current.mutate({ track: baseTrack, favorite: true });
    });
    await waitFor(() => expect(setFavorite).toHaveBeenCalledWith(1, true));
  });

  it("optimistically adds to favoriteTracks cache", async () => {
    qc.setQueryData<Track[]>(["favoriteTracks"], []);
    const { useToggleFavoriteMutation } = await import(
      "./useToggleFavoriteMutation"
    );
    const { result } = renderHook(() => useToggleFavoriteMutation(), {
      wrapper,
    });
    await act(async () => {
      result.current.mutate({ track: baseTrack, favorite: true });
    });
    const cached = qc.getQueryData<Track[]>(["favoriteTracks"]);
    expect(cached?.some((t) => t.id === 1)).toBe(true);
  });

  it("rolls back on error", async () => {
    setFavorite.mockRejectedValueOnce(new Error("network"));
    qc.setQueryData<Track[]>(["favoriteTracks"], []);
    const { useToggleFavoriteMutation } = await import(
      "./useToggleFavoriteMutation"
    );
    const { result } = renderHook(() => useToggleFavoriteMutation(), {
      wrapper,
    });
    await act(async () => {
      result.current.mutate({ track: baseTrack, favorite: true });
    });
    await waitFor(() => expect(result.current.isError).toBe(true));
    const cached = qc.getQueryData<Track[]>(["favoriteTracks"]);
    expect(cached?.some((t) => t.id === 1)).toBe(false);
  });

  it("patches isFavorite in tracks cache", async () => {
    qc.setQueryData<Track[]>(["tracks"], [{ ...baseTrack, isFavorite: false }]);
    const { useToggleFavoriteMutation } = await import(
      "./useToggleFavoriteMutation"
    );
    const { result } = renderHook(() => useToggleFavoriteMutation(), {
      wrapper,
    });
    await act(async () => {
      result.current.mutate({ track: baseTrack, favorite: true });
    });
    const cached = qc.getQueryData<Track[]>(["tracks"]);
    expect(cached?.[0]?.isFavorite).toBe(true);
  });
});
```

- [ ] **Step 2: 运行测试确认通过**

```bash
npx vitest run src/hooks/useToggleFavoriteMutation.test.ts --reporter=verbose
```

Expected: 4 tests PASS。

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useToggleFavoriteMutation.test.ts
git commit -m "test: add useToggleFavoriteMutation tests"
```

---

### Task 5: Phase 1 — SearchPage 测试补充

**Files:**
- Modify: `src/pages/SearchPage.test.tsx`
- Modify: `src/pages/SearchPage.tsx`（添加 `data-testid="search-input"`）

- [ ] **Step 1: 给搜索输入框添加 data-testid**

在 `src/pages/SearchPage.tsx` 的 `<input>` 元素上添加 `data-testid="search-input"`。

- [ ] **Step 2: 追加测试**

```ts
it("shows empty state when query is empty", async () => {
  render(<SearchPage />, { wrapper });
  expect(await screen.findByText("开始搜索")).toBeInTheDocument();
});

it("shows loading state while searching", async () => {
  search.mockReturnValue(new Promise(() => {})); // never resolves
  render(<SearchPage />, { wrapper });
  await act(async () => {
    fireEvent.change(screen.getByTestId("search-input"), {
      target: { value: "test" },
    });
  });
  expect(await screen.findByText("搜索中")).toBeInTheDocument();
});

it("shows error state on search failure", async () => {
  search.mockRejectedValue(new Error("Network error"));
  render(<SearchPage />, { wrapper });
  await act(async () => {
    fireEvent.change(screen.getByTestId("search-input"), {
      target: { value: "test" },
    });
  });
  expect(await screen.findByText("搜索失败")).toBeInTheDocument();
});

it("renders grouped results structure", async () => {
  search.mockResolvedValue({
    tracks: [],
    albums: [],
    artists: [],
    playlists: [],
  });
  render(<SearchPage />, { wrapper });
  await act(async () => {
    fireEvent.change(screen.getByTestId("search-input"), {
      target: { value: "test" },
    });
  });
  await waitFor(() => {
    expect(screen.getByText("歌曲")).toBeInTheDocument();
    expect(screen.getByText("专辑")).toBeInTheDocument();
    expect(screen.getByText("艺人")).toBeInTheDocument();
    expect(screen.getByText("播放列表")).toBeInTheDocument();
  });
});
```

- [ ] **Step 3: 运行测试确认通过**

```bash
npx vitest run src/pages/SearchPage.test.tsx --reporter=verbose
```

Expected: 6 tests PASS。

- [ ] **Step 4: Commit**

```bash
git add src/pages/SearchPage.tsx src/pages/SearchPage.test.tsx
git commit -m "test: add SearchPage loading, error, empty, and grouped results tests"
```

---

### Task 6: Phase 1 — FavoritesPage 测试补充

**Files:**
- Modify: `src/pages/FavoritesPage.test.tsx`

- [ ] **Step 1: 追加测试**

```ts
it("renders favorite tracks list", async () => {
  list.mockResolvedValue([
    { ...baseTrack, id: 1, title: "Fav Song", isFavorite: true },
  ]);
  render(<FavoritesPage />, { wrapper });
  expect(await screen.findByText("Fav Song")).toBeInTheDocument();
});

it("shows loading state", async () => {
  list.mockReturnValue(new Promise(() => {}));
  render(<FavoritesPage />, { wrapper });
  expect(await screen.findByText("加载中")).toBeInTheDocument();
});

it("shows error state", async () => {
  list.mockRejectedValue(new Error("fail"));
  render(<FavoritesPage />, { wrapper });
  expect(await screen.findByText("加载失败")).toBeInTheDocument();
});
```

（需要定义 `baseTrack` mock 和 wrapper，参考现有测试文件模式。）

- [ ] **Step 2: 运行测试确认通过**

```bash
npx vitest run src/pages/FavoritesPage.test.tsx --reporter=verbose
```

Expected: 4 tests PASS。

- [ ] **Step 3: Commit**

```bash
git add src/pages/FavoritesPage.test.tsx
git commit -m "test: add FavoritesPage loading, error, and list tests"
```

---

### Task 7: Phase 1 — RecentPlaysPage 测试补充

**Files:**
- Modify: `src/pages/RecentPlaysPage.test.tsx`

- [ ] **Step 1: 追加测试**

```ts
it("renders recent plays list", async () => {
  list.mockResolvedValue([
    { ...baseTrack, id: 1, title: "Recent Song", lastPlayedAt: 1000 },
  ]);
  render(<RecentPlaysPage />, { wrapper });
  expect(await screen.findByText("Recent Song")).toBeInTheDocument();
});

it("shows loading state", async () => {
  list.mockReturnValue(new Promise(() => {}));
  render(<RecentPlaysPage />, { wrapper });
  expect(await screen.findByText("加载中")).toBeInTheDocument();
});

it("shows error state", async () => {
  list.mockRejectedValue(new Error("fail"));
  render(<RecentPlaysPage />, { wrapper });
  expect(await screen.findByText("加载失败")).toBeInTheDocument();
});
```

- [ ] **Step 2: 运行测试确认通过**

```bash
npx vitest run src/pages/RecentPlaysPage.test.tsx --reporter=verbose
```

Expected: 4 tests PASS。

- [ ] **Step 3: Commit**

```bash
git add src/pages/RecentPlaysPage.test.tsx
git commit -m "test: add RecentPlaysPage loading, error, and list tests"
```

---

### Task 8: Phase 1 — Sidebar 测试补充

**Files:**
- Modify: `src/components/layout/Sidebar.test.tsx`

- [ ] **Step 1: 追加测试**

```ts
it("renders playlist links from query data", async () => {
  invoke.mockResolvedValue([
    { id: 1, name: "My Playlist", trackCount: 5 },
  ]);
  // 需要重新 import 以拿到最新 mock
  render(
    <QueryClientProvider client={new QueryClient({ defaultOptions: { queries: { retry: false } } })}>
      <MemoryRouter>
        <Sidebar />
      </MemoryRouter>
    </QueryClientProvider>,
  );
  expect(await screen.findByText("My Playlist")).toBeInTheDocument();
});

it("renders create playlist button", async () => {
  render(
    <QueryClientProvider client={new QueryClient({ defaultOptions: { queries: { retry: false } } })}>
      <MemoryRouter>
        <Sidebar />
      </MemoryRouter>
    </QueryClientProvider>,
  );
  expect(screen.getByRole("button", { name: "创建播放列表" })).toBeInTheDocument();
});
```

- [ ] **Step 2: 运行测试确认通过**

```bash
npx vitest run src/components/layout/Sidebar.test.tsx --reporter=verbose
```

Expected: 3 tests PASS。

- [ ] **Step 3: Commit**

```bash
git add src/components/layout/Sidebar.test.tsx
git commit -m "test: add Sidebar playlist rendering and create button tests"
```

---

### Task 9: Phase 1 — NowPlayingOverlay 测试补充

**Files:**
- Modify: `src/components/player/NowPlayingOverlay.test.tsx`

- [ ] **Step 1: 追加测试**

```ts
it("toggles play/pause on button click", async () => {
  const toggle = vi.fn();
  vi.doMock("@/hooks/usePlayer", () => ({
    usePlayer: () => ({
      ...playingSnapshot,
      toggle,
      previous: vi.fn(),
      next: vi.fn(),
      seek: vi.fn().mockResolvedValue(undefined),
      setVolume: vi.fn(),
      toggleMute: vi.fn(),
    }),
  }));
  const { NowPlayingOverlay } = await import("./NowPlayingOverlay");
  render(<NowPlayingOverlay open onClose={() => {}} />, { wrapper });
  fireEvent.click(screen.getByRole("button", { name: "暂停" }));
  expect(toggle).toHaveBeenCalledTimes(1);
});

it("renders volume slider", async () => {
  const { NowPlayingOverlay } = await import("./NowPlayingOverlay");
  render(<NowPlayingOverlay open onClose={() => {}} />, { wrapper });
  expect(screen.getByRole("slider", { name: "音量" })).toBeInTheDocument();
});

it("renders progress slider", async () => {
  const { NowPlayingOverlay } = await import("./NowPlayingOverlay");
  render(<NowPlayingOverlay open onClose={() => {}} />, { wrapper });
  expect(screen.getByRole("slider", { name: "播放进度" })).toBeInTheDocument();
});
```

- [ ] **Step 2: 运行测试确认通过**

```bash
npx vitest run src/components/player/NowPlayingOverlay.test.tsx --reporter=verbose
```

Expected: 5 tests PASS。

- [ ] **Step 3: Commit**

```bash
git add src/components/player/NowPlayingOverlay.test.tsx
git commit -m "test: add NowPlayingOverlay toggle, volume, and progress tests"
```

---

### Task 10: Phase 1 — TrackTable 测试补充

**Files:**
- Modify: `src/components/library/TrackTable.tsx`（添加 `data-testid="track-row"`）
- Modify: `src/components/library/TrackTableView.tsx`（添加 `data-testid="track-row"`）
- Modify: `src/components/library/TrackTable.test.tsx`

- [ ] **Step 1: 给 TrackTable 行添加 data-testid**

在 `TrackTableView.tsx` 的 `<tr>` 元素上添加 `data-testid="track-row"`。

- [ ] **Step 2: 追加测试**

```ts
it("renders empty list without error", () => {
  render(<TrackTable tracks={[]} queueContext="songs" />);
  expect(screen.queryAllByTestId("track-row")).toHaveLength(0);
});

it("calls onAddToPlaylist when playlist selected", () => {
  const onAddToPlaylist = vi.fn().mockResolvedValue(0);
  render(
    <TrackTable
      tracks={[baseTrack]}
      queueContext="songs"
      playlists={[{ id: 1, name: "PL", trackCount: 0, description: null, coverPath: null, createdAt: 1, updatedAt: 1 }]}
      onAddToPlaylist={onAddToPlaylist}
    />,
  );
  fireEvent.change(screen.getByRole("combobox"), { target: { value: "1" } });
  expect(onAddToPlaylist).toHaveBeenCalledWith(expect.objectContaining({ id: 1 }), 1);
});
```

- [ ] **Step 3: 运行测试确认通过**

```bash
npx vitest run src/components/library/TrackTable.test.tsx --reporter=verbose
```

Expected: 7 tests PASS。

- [ ] **Step 4: Commit**

```bash
git add src/components/library/TrackTable.tsx src/components/library/TrackTableView.tsx src/components/library/TrackTable.test.tsx
git commit -m "test: add TrackTable empty list, add-to-playlist, and data-testid"
```

---

### Task 11: Phase 1 — useKeyboardShortcuts 测试补充

**Files:**
- Modify: `src/hooks/useKeyboardShortcuts.test.tsx`

- [ ] **Step 1: 追加测试**

```ts
it("space does not toggle when Now Playing is closed and input focused", async () => {
  useUIStore.setState({ isNowPlayingOpen: false });
  const { useKeyboardShortcuts } = await import("./useKeyboardShortcuts");
  const input = document.createElement("input");
  document.body.appendChild(input);
  input.focus();
  renderHook(() => useKeyboardShortcuts(), { wrapper });
  input.dispatchEvent(new KeyboardEvent("keydown", { key: " ", bubbles: true }));
  expect(toggle).not.toHaveBeenCalled();
  input.remove();
});
```

- [ ] **Step 2: 运行测试确认通过**

```bash
npx vitest run src/hooks/useKeyboardShortcuts.test.tsx --reporter=verbose
```

Expected: 7 tests PASS。

- [ ] **Step 3: Commit**

```bash
git add src/hooks/useKeyboardShortcuts.test.tsx
git commit -m "test: add keyboard shortcut edge case for closed overlay"
```

---

### Task 12: Phase 1 — 测试验收门禁

- [ ] **Step 1: 运行全部前端测试**

```bash
npx vitest run
```

Expected: 测试数 ≥ 80，全部通过，0 unhandled rejection。

- [ ] **Step 2: 运行构建和 lint**

```bash
pnpm build && pnpm lint
```

Expected: 全部通过。

- [ ] **Step 3: 如果测试数不足 80，补充更多测试**

检查哪些模块的 checklist 项（正常渲染/loading/empty/error/成功路径/边界）未满足，逐个补充。直到测试数 ≥ 80。

---

### Task 13: Phase 2 — E2E Spike

**Files:**
- Create: `playwright.config.ts`（或 `tests/e2e/` 下配置）
- Create: `tests/e2e/spike.spec.ts`（A 档）或 `src/__tests__/e2e-integration.test.ts`（B 档）

- [ ] **Step 1: 尝试 A 档 — 安装 tauri-driver**

```bash
cargo install tauri-driver
```

检查是否安装成功。

- [ ] **Step 2: 构建 debug app**

```bash
pnpm tauri build --debug
```

确认 `.app` 产物存在。

- [ ] **Step 3: 写最小 Spike 测试（A 档）**

如果 tauri-driver 可用：

```ts
// tests/e2e/spike.spec.ts
import { test, expect } from "@playwright/test";

test("app launches and shows AppShell", async ({ page }) => {
  await page.goto("tauri://localhost");
  await expect(page.getByRole("navigation", { name: "侧边栏" })).toBeVisible();
});
```

运行：

```bash
npx playwright test tests/e2e/spike.spec.ts
```

- [ ] **Step 4: 如果 A 档失败，走 B 档**

如果 tauri-driver 不可用或不稳定，创建集成测试：

```ts
// src/__tests__/e2e-integration.test.ts
import { describe, it, expect, vi } from "vitest";
import { render, screen, act } from "@testing-library/react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { MemoryRouter } from "react-router";
import type { ReactNode } from "react";

vi.mock("@tauri-apps/api/core", () => ({
  invoke: vi.fn().mockResolvedValue([]),
}));
vi.mock("@tauri-apps/api/event", () => ({
  listen: vi.fn().mockResolvedValue(() => {}),
}));

const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
const wrapper = ({ children }: { children: ReactNode }) => (
  <QueryClientProvider client={qc}><MemoryRouter>{children}</MemoryRouter></QueryClientProvider>
);

describe("E2E integration (B 档)", () => {
  it("renders AppShell with sidebar and player", async () => {
    const { AppShell } = await import("@/components/layout/AppShell");
    await act(async () => {
      render(<AppShell />, { wrapper });
    });
    expect(screen.getByRole("navigation", { name: "侧边栏" })).toBeInTheDocument();
    expect(screen.getByRole("contentinfo", { name: "播放器" })).toBeInTheDocument();
  });
});
```

- [ ] **Step 5: Commit**

```bash
git add tests/e2e/ 或 src/__tests__/e2e-integration.test.ts
git commit -m "test: add E2E spike (A or B tier)"
```

---

### Task 14: Phase 2 — E2E 五条核心链路（根据 Spike 结果选择 A 档或 B 档）

**Files:**
- Create: `tests/e2e/*.spec.ts`（A 档）或 `src/__tests__/e2e-*.test.ts`（B 档）

- [ ] **Step 1: 链路 1 — 扫描 → 浏览**

（A 档用 Playwright，B 档用 Vitest 集成测试。以下以 B 档为例。）

```ts
it("scan → browse: seed library and verify tracks appear", async () => {
  // mock invoke 返回种子数据
  // 渲染 SongsPage
  // 断言曲目列表非空
});
```

- [ ] **Step 2: 链路 2 — 播放控制**

```ts
it("playback: click play, verify MiniPlayer shows track info", async () => {
  // 渲染 SongsPage + AppShell
  // 点击播放按钮
  // 断言 MiniPlayer 显示曲目标题
});
```

- [ ] **Step 3: 链路 3 — 搜索 → 收藏**

```ts
it("search → favorite: search and add to favorites", async () => {
  // 渲染 SearchPage
  // 输入搜索词
  // 点击收藏按钮
  // 断言收藏缓存已更新
});
```

- [ ] **Step 4: 链路 4 — 播放列表 CRUD**

```ts
it("playlist CRUD: create, add track, verify", async () => {
  // 渲染 PlaylistsPage
  // 创建播放列表
  // 添加曲目
  // 断言曲目出现
});
```

- [ ] **Step 5: 链路 5 — Now Playing**

```ts
it("now playing: open overlay, verify controls, close with Esc", async () => {
  // 渲染 AppShell + 播放状态
  // 点击 MiniPlayer 区域
  // 断言 Now Playing 对话框可见
  // 按 Esc
  // 断言 Now Playing 关闭
});
```

- [ ] **Step 6: 运行全部 E2E 测试**

A 档：`npx playwright test`
B 档：`npx vitest run src/__tests__/e2e-*.test.ts`

Expected: 全部通过。

- [ ] **Step 7: Commit**

```bash
git add tests/e2e/ 或 src/__tests__/
git commit -m "test: add 5 core E2E integration test flows"
```

---

### Task 15: Phase 2 — E2E 验收

- [ ] **Step 1: 确认 `pnpm tauri build --debug` 通过**

- [ ] **Step 2: 确认 E2E 测试全部通过**

- [ ] **Step 3: 确认无 flaky（运行 3 次验证稳定性）**

---

### Task 16: Phase 3 — TrackTable 虚拟滚动

**Files:**
- Modify: `src/components/library/TrackTableView.tsx`
- Modify: `src/components/library/TrackTable.test.tsx`

- [ ] **Step 1: 安装依赖**

```bash
pnpm add @tanstack/react-virtual
```

- [ ] **Step 2: 实现虚拟滚动**

在 `TrackTableView.tsx` 中：
- 使用 `useVirtualizer` 替代 `rows.map()`
- 外层 `<tbody>` 替换为虚拟滚动容器
- 每行渲染保持 `data-testid="track-row"`
- 保留 feature flag：`props.virtual?: boolean`，默认 true，可降级为 false 走原有 map

- [ ] **Step 3: 补回受影响的测试**

更新 `TrackTable.test.tsx`：
- 确保现有测试在虚拟模式下通过
- 添加非虚拟降级测试

- [ ] **Step 4: 运行测试确认通过**

```bash
npx vitest run src/components/library/TrackTable.test.tsx --reporter=verbose
```

- [ ] **Step 5: Commit**

```bash
git add src/components/library/TrackTableView.tsx src/components/library/TrackTable.test.tsx pnpm-lock.yaml
git commit -m "feat: add virtual scrolling to TrackTable with feature flag"
```

---

### Task 17: Phase 3 — React Query staleTime 优化

**Files:**
- Modify: 各 `*Repo.ts` 或 `QueryClientProvider` 配置

- [ ] **Step 1: 检查当前 staleTime 配置**

搜索 `staleTime`、`QueryClient` 默认配置。

- [ ] **Step 2: 合理设置 staleTime**

对不频繁变化的数据设置 `staleTime`：
- `playlists`：60s
- `albums`、`artists`：120s
- `tracks`：30s
- `search`：0（实时）

- [ ] **Step 3: 验证页面切换不重复请求**

手动测试：进入"歌曲"页面 → 导航到"专辑" → 返回"歌曲" → 检查 Network 面板无重复请求。

- [ ] **Step 4: Commit**

```bash
git add src/
git commit -m "perf: set staleTime for track/album/artist/playlist queries"
```

---

### Task 18: Phase 3 — Now Playing 焦点陷阱

**Files:**
- Modify: `src/components/player/NowPlayingOverlay.tsx`
- Modify: `src/components/player/NowPlayingOverlay.test.tsx`

- [ ] **Step 1: 安装 focus-trap-react**

```bash
pnpm add focus-trap-react
```

- [ ] **Step 2: 实现焦点陷阱**

在 `NowPlayingOverlay.tsx` 中：
- 当 `open` 为 true 时，用 `<FocusTrap>` 包裹内容
- 关闭时恢复焦点到触发元素

- [ ] **Step 3: 补回测试**

```ts
it("traps focus inside overlay when open", async () => {
  const { NowPlayingOverlay } = await import("./NowPlayingOverlay");
  render(<NowPlayingOverlay open onClose={() => {}} />, { wrapper });
  // Tab 遍历验证焦点不逃逸
});
```

- [ ] **Step 4: 运行测试确认通过**

```bash
npx vitest run src/components/player/NowPlayingOverlay.test.tsx --reporter=verbose
```

- [ ] **Step 5: Commit**

```bash
git add src/components/player/NowPlayingOverlay.tsx src/components/player/NowPlayingOverlay.test.tsx pnpm-lock.yaml
git commit -m "a11y: add focus trap to NowPlayingOverlay"
```

---

### Task 19: Phase 3 — 可访问性修复（aria-label + 对比度 + 点击目标）

**Files:**
- Modify: `src/components/layout/Sidebar.tsx`
- Modify: `src/components/player/MiniPlayer.tsx`
- Modify: `src/components/player/NowPlayingOverlay.tsx`
- Modify: `src/styles/globals.css`

- [ ] **Step 1: 检查缺失 aria-label 的控件**

```bash
grep -rn "button\|input\|select" src/components/ | grep -v "aria-label" | grep -v "test" | head -20
```

为缺失的控件添加 `aria-label`。

- [ ] **Step 2: 检查点击目标尺寸**

```bash
grep -rn "width.*[0-9]\|height.*[0-9]" src/styles/globals.css | grep "button"
```

确保图标按钮 ≥ 40px，主要播放按钮 ≥ 44px。

- [ ] **Step 3: 检查文本对比度**

用 Chrome DevTools Lighthouse 面板运行可访问性审计，修复对比度 < 4.5:1 的文本。

- [ ] **Step 4: 运行测试确认无回归**

```bash
npx vitest run
```

- [ ] **Step 5: Commit**

```bash
git add src/components/ src/styles/globals.css
git commit -m "a11y: fix aria labels, contrast, and click target sizes"
```

---

### Task 20: Phase 4 — 回归门禁 + 版本发布

- [ ] **Step 1: 运行全部前端门禁**

```bash
npx vitest run
pnpm build
pnpm lint
```

Expected: 全部通过，测试数 ≥ 80。

- [ ] **Step 2: 运行 Rust 门禁**

```bash
source ~/.cargo/env
cargo fmt --manifest-path src-tauri/Cargo.toml --check
CARGO_TARGET_DIR=/tmp/musicplayer-target cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
CARGO_TARGET_DIR=/tmp/musicplayer-target cargo test --manifest-path src-tauri/Cargo.toml
```

Expected: 全部通过。

- [ ] **Step 3: 运行 Tauri build 门禁**

```bash
pnpm tauri build --debug
```

Expected: `.app` 产物生成。

- [ ] **Step 4: 运行 E2E 门禁**

A 档：`npx playwright test`
B 档：`npx vitest run src/__tests__/e2e-*.test.ts`

Expected: 全部通过。

- [ ] **Step 5: 版本升级**

更新 `package.json`、`Cargo.toml`、`tauri.conf.json` 版本号为 `0.8.0`。

- [ ] **Step 6: 合并到 main 并打 tag**

```bash
git checkout main
git merge feat/v0.8.0-quality-hardening --no-ff -m "feat: v0.8.0 quality hardening"
git tag v0.8.0
```

- [ ] **Step 7: 推送到 GitHub**

```bash
git push origin main --tags
```

---

## Spec Coverage Self-Review

| Spec 要求 | Task 覆盖 |
|---|---|
| 测试数 ≥ 80 + checklist | Task 2-12 |
| E2E A/B 档 + Spike | Task 13-15 |
| 首屏加载 < 2s | Task 1（基线）+ Task 20（门禁） |
| 虚拟滚动 DOM row ≤ 80 | Task 16 |
| React Query staleTime | Task 17 |
| Now Playing 焦点陷阱 | Task 18 |
| aria-label + 对比度 + 点击目标 | Task 19 |
| `data-testid="track-row"` | Task 10 |
| `data-testid="search-input"` | Task 5 |
| 定位器规范（role > testid > CSS） | Task 14（E2E 链路） |
| `pnpm tauri build --debug` 门禁 | Task 15 + Task 20 |
| 测试数据隔离 | Task 14（每条链路独立 mock） |
| 虚拟滚动 feature flag | Task 16 |
| 最终门禁 8 项命令 | Task 20 |
| 版本号 0.8.0 | Task 20 |

## Placeholder Scan

已检查：无 TBD / TODO / "implement later"。所有步骤包含具体代码或命令。

## Type Consistency

- `useToggleFavoriteMutation` 签名：`({ track, favorite }: { track: Track; favorite: boolean })`
- `TrackTableRow.isFavorite`：可选 boolean
- `data-testid` 值：`track-row`、`now-playing-cover`、`search-input`

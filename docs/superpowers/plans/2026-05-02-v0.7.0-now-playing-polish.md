# MusicPlayer v0.7.0 Now Playing 大屏与 Apple Music 体验强化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 v0.6.0 基础上交付可验收的 v0.7.0 体验层：Now Playing 大屏、连续转场、快捷键冲突矩阵、主题跟随与 i18n 收敛。

**Architecture:** 继续保持 `src/repositories/` 为 IPC 边界，播放器状态仍由 `usePlayerStore` 单源驱动。新增 `NowPlayingOverlay` + `NowPlayingTransition` 负责 MiniPlayer 展开体验，新增 `useKeyboardShortcuts` 与 `useSystemTheme` 负责全局行为，不改动 Rust 音频内核与数据库模型。媒体键不走前端快捷键路径，沿用 `system::now_playing` 作为唯一系统桥接入口并做回归验证。

**Tech Stack:** React 19, TypeScript strict, React Router 7, Zustand, TanStack Query 5, Framer Motion, i18next/react-i18next, Vitest + Testing Library, Tauri 2, Rust (existing tests only for regression).

---

## Scope Check

本 spec 虽覆盖多个点（Now Playing、快捷键、主题、i18n），但都属于同一子系统“播放器体验层”，并共享同一改动主线（AppShell + Player + 全局行为），可在一份实现计划内完成，不需要再拆多份 plan。

## Revised Conclusion

这版计划已可开工，但必须先把“类型一致性、转场实现、快捷键冲突矩阵、主题降级、i18n 结构校验”作为硬前置。下方任务已把这些 blocker 内化为必做步骤，不允许跳过。

## File Structure

Create:

- `src/components/player/NowPlayingOverlay.tsx` — Now Playing 大屏容器与交互。
- `src/components/player/NowPlayingBackground.tsx` — 取色渐变 + 模糊层 + 玻璃控制区背景。
- `src/components/player/NowPlayingOverlay.test.tsx` — 大屏结构完整性、空状态、控制区测试。
- `src/components/player/NowPlayingBackground.test.tsx` — 背景层降级、玻璃层和半透明样式测试。
- `src/hooks/useKeyboardShortcuts.ts` — Space / Cmd+F / Esc 规则与冲突处理。
- `src/hooks/useKeyboardShortcuts.test.tsx` — 快捷键冲突矩阵测试。
- `src/hooks/useSystemTheme.ts` — 系统主题监听与同步。
- `src/hooks/useSystemTheme.test.tsx` — 主题同步测试。
- `src/stores/uiStore.test.ts` — overlay/open-close 与主题状态测试。
- `src/i18n/locales/en.json` — v0.7 对应英文 key 结构（可先基础文案）。
- `src/i18n/keyParity.test.ts` — zh/en key 结构一致性测试。

Modify:

- `src/stores/uiStore.ts` — 增加 Now Playing 与主题状态 action。
- `src/components/layout/AppShell.tsx` — 挂载 overlay + 快捷键 + 主题 hook。
- `src/components/player/MiniPlayer.tsx` — 增加可展开交互、共享布局标记。
- `src/components/player/index.ts` — 导出 NowPlayingOverlay。
- `src/hooks/index.ts` — 导出新增 hooks。
- `src/pages/SearchPage.tsx` — 支持 `?focus=1` 后自动聚焦输入框。
- `src/pages/SearchPage.test.tsx` — 增加 focus 场景测试。
- `src/i18n/index.ts` — 加入 `en` 资源、基础配置补齐。
- `src/main.tsx` — 顶部 side-effect import `@/i18n`。
- `src/i18n/locales/zh.json` — 补齐 v0.6/v0.7 新 key。
- `src/components/layout/Sidebar.tsx`、`src/components/player/MiniPlayer.tsx`、`src/components/player/NowPlayingOverlay.tsx`、`src/pages/{SearchPage,FavoritesPage,RecentPlaysPage}.tsx` — 替换硬编码文案为 i18n。
- `src/styles/globals.css` — Now Playing 视觉、转场、玻璃层、状态反馈样式。
- `src/components/layout/AppShell.test.tsx` — 断言 overlay 挂载与基础可见性。
- `src-tauri/src/system/now_playing.rs` — 如需最小回归断言补强（仅测试，不改行为）。

## Conventions

- 在独立 worktree 开发：

```bash
git worktree add .worktrees/v0.7.0-now-playing-polish -b feat/v0.7.0-now-playing-polish
cd .worktrees/v0.7.0-now-playing-polish
```

- 所有新增 IPC 调用必须继续走 `src/repositories/`（本计划预期不新增 IPC）。
- 媒体键不在前端实现，避免与 Rust `system::now_playing` 双轨。
- 统一使用 `CARGO_TARGET_DIR=/tmp/musicplayer-target` 跑 Rust 命令。

---

### Task 1: UI 状态基础（Now Playing 开关 + 主题状态）

**Files:**
- Modify: `src/stores/uiStore.ts`
- Create: `src/stores/uiStore.test.ts`

- [ ] **Step 1: 写失败测试（先定义状态契约）**

`src/stores/uiStore.test.ts`:

```ts
import { beforeEach, describe, expect, it } from "vitest";
import { useUIStore } from "./uiStore";

describe("uiStore", () => {
  beforeEach(() => {
    useUIStore.setState({
      isNowPlayingOpen: false,
      sidebarCollapsed: false,
      theme: "system",
      resolvedTheme: "light",
    });
  });

  it("opens and closes now playing", () => {
    useUIStore.getState().openNowPlaying();
    expect(useUIStore.getState().isNowPlayingOpen).toBe(true);
    useUIStore.getState().closeNowPlaying();
    expect(useUIStore.getState().isNowPlayingOpen).toBe(false);
  });

  it("resolves theme from system", () => {
    useUIStore.getState().setResolvedTheme("dark");
    expect(useUIStore.getState().resolvedTheme).toBe("dark");
  });
});
```

- [ ] **Step 2: 运行测试，确认失败**

Run:

```bash
pnpm vitest run src/stores/uiStore.test.ts
```

Expected: FAIL，提示 `openNowPlaying/closeNowPlaying/setResolvedTheme` 不存在。

- [ ] **Step 3: 最小实现状态与 action**

`src/stores/uiStore.ts`:

```ts
import { create } from "zustand";

type ResolvedTheme = "light" | "dark";
type ThemeMode = "system";

interface UIState {
  isNowPlayingOpen: boolean;
  sidebarCollapsed: boolean;
  theme: ThemeMode;
  resolvedTheme: ResolvedTheme;
  openNowPlaying: () => void;
  closeNowPlaying: () => void;
  toggleNowPlaying: () => void;
  setResolvedTheme: (theme: ResolvedTheme) => void;
}

export const useUIStore = create<UIState>((set) => ({
  isNowPlayingOpen: false,
  sidebarCollapsed: false,
  theme: "system",
  resolvedTheme: "light",
  openNowPlaying: () => set({ isNowPlayingOpen: true }),
  closeNowPlaying: () => set({ isNowPlayingOpen: false }),
  toggleNowPlaying: () =>
    set((state) => ({ isNowPlayingOpen: !state.isNowPlayingOpen })),
  setResolvedTheme: (resolvedTheme) => set({ resolvedTheme }),
}));
```

- [ ] **Step 4: 重跑测试，确认通过**

Run:

```bash
pnpm vitest run src/stores/uiStore.test.ts
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/stores/uiStore.ts src/stores/uiStore.test.ts
git commit -m "feat: add ui store actions for now playing and theme"
```

---

### Task 2: Now Playing Overlay（先锁类型，再落 UI 与共享转场）

**Files:**
- Create: `src/components/player/NowPlayingOverlay.tsx`
- Create: `src/components/player/NowPlayingOverlay.test.tsx`
- Modify: `src/components/player/index.ts`
- Modify: `src/components/player/MiniPlayer.tsx`

- [ ] **Step 0: 先核对真实类型契约（必做）**

先读：

```bash
sed -n '1,220p' src/repositories/playerRepo.ts
sed -n '1,260p' src/stores/playerStore.ts
sed -n '1,260p' src/hooks/usePlayer.ts
```

要求：

1. 测试 mock 必须复用当前 `PlayerSnapshot` / `NowPlayingTrack` 字段命名。
2. 禁止在测试里自造第二套 `current` shape。

- [ ] **Step 1: 写失败测试（完整结构 + 空状态 + 关闭按钮）**

`src/components/player/NowPlayingOverlay.test.tsx`:

```tsx
import { fireEvent, render, screen } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";
import type { PlayerSnapshot } from "@/repositories/playerRepo";

const player = {
  toggle: vi.fn(),
  previous: vi.fn(),
  next: vi.fn(),
  seek: vi.fn(),
  setVolume: vi.fn(),
  toggleMute: vi.fn(),
};

const playingSnapshot: PlayerSnapshot = {
  status: "playing",
  current: {
    id: 1,
    title: "Track A",
    artistName: "Artist A",
    albumName: "Album A",
    durationMs: 180000,
    coverPath: null,
  },
  positionMs: 30000,
  durationMs: 180000,
  volume: 0.8,
  muted: false,
  queueIndex: 0,
  queueLen: 3,
  repeatMode: "off",
  shuffle: false,
};

let currentSnapshot: PlayerSnapshot = playingSnapshot;

vi.mock("@/hooks/usePlayer", () => ({
  usePlayer: () => ({ ...currentSnapshot, ...player }),
}));

describe("NowPlayingOverlay", () => {
  it("renders full structure when open", async () => {
    const { NowPlayingOverlay } = await import("./NowPlayingOverlay");
    const onClose = vi.fn();
    render(<NowPlayingOverlay open onClose={onClose} />);
    expect(screen.getByRole("dialog", { name: "Now Playing" })).toBeInTheDocument();
    expect(screen.getByTestId("now-playing-cover")).toBeInTheDocument();
    expect(screen.getByText("Track A")).toBeInTheDocument();
    expect(screen.getByText("Artist A")).toBeInTheDocument();
    expect(screen.getByText("Album A")).toBeInTheDocument();
    expect(screen.getByRole("slider", { name: "播放进度" })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: "上一首" })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: "播放" })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: "下一首" })).toBeInTheDocument();
    expect(screen.getByRole("slider", { name: "音量" })).toBeInTheDocument();
    fireEvent.click(screen.getByRole("button", { name: "关闭 Now Playing" }));
    expect(onClose).toHaveBeenCalledTimes(1);
  });
});
```

补充空状态测试（同文件）：

```tsx
it("renders empty state when no current track", async () => {
  currentSnapshot = { ...playingSnapshot, current: null, status: "idle" };
  const { NowPlayingOverlay } = await import("./NowPlayingOverlay");
  render(<NowPlayingOverlay open onClose={() => {}} />);
  expect(screen.getByText("暂无播放内容")).toBeInTheDocument();
});
```

- [ ] **Step 2: 运行测试，确认失败**

Run:

```bash
pnpm vitest run src/components/player/NowPlayingOverlay.test.tsx
```

Expected: FAIL（文件/结构/按钮未实现）。

- [ ] **Step 3: 实现 Overlay 与 MiniPlayer 共享转场（Framer Motion 正确写法）**

`src/components/player/NowPlayingOverlay.tsx`（核心片段）:

```tsx
import { AnimatePresence, LayoutGroup, motion } from "framer-motion";
import { usePlayer } from "@/hooks/usePlayer";
import { NowPlayingBackground } from "./NowPlayingBackground";

export function NowPlayingOverlay({ open, onClose }: { open: boolean; onClose: () => void }) {
  const player = usePlayer();
  const current = player.current;

  return (
    <AnimatePresence>
      {open ? (
        <LayoutGroup>
          <motion.section
            className="now-playing"
            role="dialog"
            aria-label="Now Playing"
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
          >
            <NowPlayingBackground coverPath={current?.coverPath ?? null} />
            <header className="now-playing__header">
              <button type="button" onClick={onClose} aria-label="关闭 Now Playing">
                返回
              </button>
            </header>
            {current ? (
              <>
                <motion.div
                  layoutId="current-cover"
                  data-testid="now-playing-cover"
                  className="now-playing__cover"
                />
                {/* title / artist / album / progress / prev-play-next / volume */}
              </>
            ) : (
              <div className="now-playing__empty">暂无播放内容</div>
            )}
          </motion.section>
        </LayoutGroup>
      ) : null}
    </AnimatePresence>
  );
}
```

`src/components/player/MiniPlayer.tsx`（片段）:

```tsx
import { motion } from "framer-motion";
import { useUIStore } from "@/stores/uiStore";
const openNowPlaying = useUIStore((s) => s.openNowPlaying);
<div className="mini-player__track" role="button" tabIndex={0} onClick={openNowPlaying}>
  <motion.div layoutId="current-cover" className="mini-player__art" aria-hidden="true" />
  <div className="mini-player__meta">
    <div className="mini-player__title">{title}</div>
    <div className="mini-player__subtitle">{subtitle}</div>
  </div>
</div>
```

注意：Overlay 不注册 `window.keydown` 的 Esc 监听。Esc 统一由 Task 4 的 `useKeyboardShortcuts` 处理。

- [ ] **Step 4: 重跑测试，确认通过**

Run:

```bash
pnpm vitest run src/components/player/NowPlayingOverlay.test.tsx
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/components/player/NowPlayingOverlay.tsx src/components/player/NowPlayingOverlay.test.tsx src/components/player/MiniPlayer.tsx src/components/player/index.ts
git commit -m "feat: add now playing overlay with shared-layout transition"
```

---

### Task 3: AppShell 挂载 Overlay 与背景层降级策略

**Files:**
- Modify: `src/components/layout/AppShell.tsx`
- Modify: `src/components/layout/AppShell.test.tsx`
- Create: `src/components/player/NowPlayingBackground.tsx`
- Create: `src/components/player/NowPlayingBackground.test.tsx`

- [ ] **Step 1: 写失败测试（带 store reset，避免污染）**

在 `src/components/layout/AppShell.test.tsx` 中增加：

```tsx
import { beforeEach, describe, expect, it } from "vitest";
import { useUIStore } from "@/stores/uiStore";

beforeEach(() => {
  useUIStore.setState({
    isNowPlayingOpen: false,
    sidebarCollapsed: false,
    theme: "system",
    resolvedTheme: "light",
  });
});

it("does not render now playing overlay by default", () => {
  expect(screen.queryByRole("dialog", { name: "Now Playing" })).not.toBeInTheDocument();
});

it("renders now playing overlay when ui state is open", () => {
  useUIStore.setState({ isNowPlayingOpen: true });
  expect(screen.getByRole("dialog", { name: "Now Playing" })).toBeInTheDocument();
});
```

- [ ] **Step 2: 运行测试，确认失败**

Run:

```bash
pnpm vitest run src/components/layout/AppShell.test.tsx
```

Expected: FAIL（AppShell 还未挂载 overlay）。

- [ ] **Step 3: 实现 AppShell 挂载与背景降级**

`src/components/layout/AppShell.tsx`（核心片段）:

```tsx
import { NowPlayingOverlay } from "@/components/player";
import { useUIStore } from "@/stores/uiStore";
import { useKeyboardShortcuts } from "@/hooks/useKeyboardShortcuts";
import { useSystemTheme } from "@/hooks/useSystemTheme";

export function AppShell() {
  const isNowPlayingOpen = useUIStore((s) => s.isNowPlayingOpen);
  const closeNowPlaying = useUIStore((s) => s.closeNowPlaying);
  useKeyboardShortcuts();
  useSystemTheme();
  return (
    <div className="app-shell">
      {/* existing layout */}
      <NowPlayingOverlay open={isNowPlayingOpen} onClose={closeNowPlaying} />
    </div>
  );
}
```

`src/components/player/NowPlayingBackground.tsx`（核心片段）:

```tsx
export function NowPlayingBackground({ coverPath }: { coverPath: string | null }) {
  return (
    <div className="np-bg">
      <div className="np-bg__gradient" />
      <div
        className="np-bg__blurred-cover"
        style={coverPath ? { backgroundImage: `url(${coverPath})` } : undefined}
      />
      <div className="np-bg__glass" />
    </div>
  );
}
```

实现约束：

1. 有 `coverPath`：封面放大模糊 + 固定渐变遮罩。
2. 无 `coverPath`：默认稳定渐变（不依赖运行时取色库）。
3. `extract-colors` 不在 v0.7 必做范围内。

- [ ] **Step 4: 重跑测试，确认通过**

Run:

```bash
pnpm vitest run src/components/layout/AppShell.test.tsx src/components/player/NowPlayingBackground.test.tsx
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/components/layout/AppShell.tsx src/components/layout/AppShell.test.tsx src/components/player/NowPlayingBackground.tsx src/components/player/NowPlayingBackground.test.tsx
git commit -m "feat: mount now playing overlay and background layers in app shell"
```

---

### Task 4: 全局快捷键冲突矩阵（Space / Cmd+F / Esc）

**Files:**
- Create: `src/hooks/useKeyboardShortcuts.ts`
- Create: `src/hooks/useKeyboardShortcuts.test.tsx`
- Modify: `src/pages/SearchPage.tsx`
- Modify: `src/pages/SearchPage.test.tsx`
- Modify: `src/hooks/index.ts`

- [ ] **Step 1: 写失败测试（完整断言，不留半成品）**

`src/hooks/useKeyboardShortcuts.test.tsx` 至少覆盖 6 个用例：

```tsx
import { renderHook } from "@testing-library/react";
import { beforeEach, describe, expect, it, vi } from "vitest";
import { useUIStore } from "@/stores/uiStore";

const toggle = vi.fn();
const navigate = vi.fn();

vi.mock("@/hooks/usePlayer", () => ({ usePlayer: () => ({ toggle }) }));
vi.mock("react-router", async (orig) => {
  const actual = await orig();
  return { ...actual, useNavigate: () => navigate };
});

describe("useKeyboardShortcuts", () => {
  beforeEach(() => {
    toggle.mockReset();
    navigate.mockReset();
    useUIStore.setState({ isNowPlayingOpen: true, sidebarCollapsed: false, theme: "system", resolvedTheme: "light" });
    delete (window as { __TAURI_INTERNALS__?: unknown }).__TAURI_INTERNALS__;
  });

  it("ignores space when input is focused", async () => {
    const { useKeyboardShortcuts } = await import("./useKeyboardShortcuts");
    const input = document.createElement("input");
    document.body.appendChild(input);
    input.focus();
    renderHook(() => useKeyboardShortcuts());
    window.dispatchEvent(new KeyboardEvent("keydown", { key: " ", bubbles: true }));
    expect(toggle).not.toHaveBeenCalled();
  });

  it("toggles playback on space in normal context", async () => {
    const { useKeyboardShortcuts } = await import("./useKeyboardShortcuts");
    renderHook(() => useKeyboardShortcuts());
    window.dispatchEvent(new KeyboardEvent("keydown", { key: " ", bubbles: true }));
    expect(toggle).toHaveBeenCalledTimes(1);
  });

  it("ignores space when slider is focused", async () => {
    const { useKeyboardShortcuts } = await import("./useKeyboardShortcuts");
    const slider = document.createElement("div");
    slider.setAttribute("role", "slider");
    document.body.appendChild(slider);
    slider.focus();
    renderHook(() => useKeyboardShortcuts());
    slider.dispatchEvent(new KeyboardEvent("keydown", { key: " ", bubbles: true }));
    expect(toggle).not.toHaveBeenCalled();
  });

  it("handles cmd+f in tauri runtime", async () => {
    (window as { __TAURI_INTERNALS__?: unknown }).__TAURI_INTERNALS__ = {};
    const { useKeyboardShortcuts } = await import("./useKeyboardShortcuts");
    renderHook(() => useKeyboardShortcuts());
    const event = new KeyboardEvent("keydown", { key: "f", metaKey: true, bubbles: true, cancelable: true });
    window.dispatchEvent(event);
    expect(navigate).toHaveBeenCalledWith("/search?focus=1");
    expect(event.defaultPrevented).toBe(true);
  });

  it("does not hijack cmd+f in web runtime", async () => {
    const { useKeyboardShortcuts } = await import("./useKeyboardShortcuts");
    renderHook(() => useKeyboardShortcuts());
    const event = new KeyboardEvent("keydown", { key: "f", metaKey: true, bubbles: true, cancelable: true });
    window.dispatchEvent(event);
    expect(navigate).not.toHaveBeenCalled();
    expect(event.defaultPrevented).toBe(false);
  });

  it("esc closes now playing", async () => {
    const { useKeyboardShortcuts } = await import("./useKeyboardShortcuts");
    const input = document.createElement("input");
    input.value = "keep-me";
    document.body.appendChild(input);
    input.focus();
    renderHook(() => useKeyboardShortcuts());
    window.dispatchEvent(new KeyboardEvent("keydown", { key: "Escape", bubbles: true }));
    expect(useUIStore.getState().isNowPlayingOpen).toBe(false);
    expect(input.value).toBe("keep-me");
  });
});
```

- [ ] **Step 2: 运行测试，确认失败**

Run:

```bash
pnpm vitest run src/hooks/useKeyboardShortcuts.test.tsx
```

Expected: FAIL（hook 未实现或断言不满足）。

- [ ] **Step 3: 实现 hook（TS strict 安全 + 单源 Esc）**

`src/hooks/useKeyboardShortcuts.ts`（核心片段）:

```ts
import { useEffect } from "react";
import { useNavigate } from "react-router";
import { usePlayer } from "@/hooks/usePlayer";
import { useUIStore } from "@/stores/uiStore";

function isTauriRuntime() {
  return "__TAURI_INTERNALS__" in window;
}

function shouldIgnoreSpace(target: EventTarget | null) {
  if (!(target instanceof HTMLElement)) return false;
  if (target.closest('[role="slider"]')) return true;
  return target.matches("input, textarea, [contenteditable='true']");
}

export function useKeyboardShortcuts() {
  const navigate = useNavigate();
  const player = usePlayer();
  const closeNowPlaying = useUIStore((s) => s.closeNowPlaying);

  useEffect(() => {
    const onKeyDown = (e: KeyboardEvent) => {
      if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "f") {
        if (isTauriRuntime()) {
          e.preventDefault();
          navigate("/search?focus=1");
        }
        return;
      }
      if (e.key === "Escape") {
        closeNowPlaying();
        return;
      }
      if (e.key === " " && !shouldIgnoreSpace(e.target)) {
        e.preventDefault();
        player.toggle();
      }
    };
    window.addEventListener("keydown", onKeyDown);
    return () => window.removeEventListener("keydown", onKeyDown);
  }, [navigate, player, closeNowPlaying]);
}
```

`src/pages/SearchPage.tsx`：保留 `?focus=1` 聚焦逻辑，输入框必须绑定 `ref={inputRef}`。

- [ ] **Step 4: 重跑测试，确认通过**

Run:

```bash
pnpm vitest run src/hooks/useKeyboardShortcuts.test.tsx src/pages/SearchPage.test.tsx
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useKeyboardShortcuts.ts src/hooks/useKeyboardShortcuts.test.tsx src/pages/SearchPage.tsx src/pages/SearchPage.test.tsx src/hooks/index.ts
git commit -m "feat: implement global keyboard shortcuts conflict matrix"
```

---

### Task 5: 系统主题跟随（含无 matchMedia 降级）

**Files:**
- Create: `src/hooks/useSystemTheme.ts`
- Create: `src/hooks/useSystemTheme.test.tsx`
- Modify: `src/hooks/index.ts`
- Modify: `src/components/layout/AppShell.tsx`

- [ ] **Step 1: 写失败测试（正常路径 + 无 matchMedia 降级）**

`src/hooks/useSystemTheme.test.tsx`:

```tsx
import { renderHook } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";
import { useUIStore } from "@/stores/uiStore";

describe("useSystemTheme", () => {
  it("syncs resolved theme with system preference", async () => {
    const listeners: Array<(e: MediaQueryListEvent) => void> = [];
    vi.stubGlobal("matchMedia", (_query: string) => ({
      matches: false,
      media: "(prefers-color-scheme: dark)",
      addEventListener: (_: string, cb: (e: MediaQueryListEvent) => void) => listeners.push(cb),
      removeEventListener: vi.fn(),
    }));
    const { useSystemTheme } = await import("./useSystemTheme");
    renderHook(() => useSystemTheme());
    listeners.forEach((cb) => cb({ matches: true } as MediaQueryListEvent));
    expect(useUIStore.getState().resolvedTheme).toBe("dark");
  });

  it("falls back to light when matchMedia is unavailable", async () => {
    vi.stubGlobal("matchMedia", undefined);
    const { useSystemTheme } = await import("./useSystemTheme");
    renderHook(() => useSystemTheme());
    expect(useUIStore.getState().resolvedTheme).toBe("light");
  });
});
```

- [ ] **Step 2: 运行测试，确认失败**

Run:

```bash
pnpm vitest run src/hooks/useSystemTheme.test.tsx
```

Expected: FAIL（hook 未实现或缺降级分支）。

- [ ] **Step 3: 实现 hook（带 fallback）**

`src/hooks/useSystemTheme.ts`（核心片段）:

```ts
import { useEffect } from "react";
import { useUIStore } from "@/stores/uiStore";

export function useSystemTheme() {
  const setResolvedTheme = useUIStore((s) => s.setResolvedTheme);
  const resolvedTheme = useUIStore((s) => s.resolvedTheme);

  useEffect(() => {
    if (typeof window.matchMedia !== "function") {
      setResolvedTheme("light");
      return;
    }
    const mq = window.matchMedia("(prefers-color-scheme: dark)");
    const apply = (isDark: boolean) => setResolvedTheme(isDark ? "dark" : "light");
    apply(mq.matches);
    const onChange = (e: MediaQueryListEvent) => apply(e.matches);
    mq.addEventListener("change", onChange);
    return () => mq.removeEventListener("change", onChange);
  }, [setResolvedTheme]);

  useEffect(() => {
    document.documentElement.dataset.theme = resolvedTheme;
  }, [resolvedTheme]);
}
```

- [ ] **Step 4: 重跑测试，确认通过**

Run:

```bash
pnpm vitest run src/hooks/useSystemTheme.test.tsx
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/hooks/useSystemTheme.ts src/hooks/useSystemTheme.test.tsx src/components/layout/AppShell.tsx src/hooks/index.ts
git commit -m "feat: add system theme hook with matchMedia fallback"
```

---

### Task 6: i18n 收敛（含 zh/en key parity）

**Files:**
- Modify: `src/main.tsx`
- Modify: `src/i18n/index.ts`
- Modify: `src/i18n/locales/zh.json`
- Create: `src/i18n/locales/en.json`
- Create: `src/i18n/keyParity.test.ts`
- Modify: `src/components/layout/Sidebar.tsx`
- Modify: `src/components/player/MiniPlayer.tsx`
- Modify: `src/components/player/NowPlayingOverlay.tsx`
- Modify: `src/pages/SearchPage.tsx`
- Modify: `src/pages/FavoritesPage.tsx`
- Modify: `src/pages/RecentPlaysPage.tsx`
- Modify: `src/pages/{SearchPage,FavoritesPage,RecentPlaysPage}.test.tsx`

- [ ] **Step 1: 写失败测试（key parity + 关键文案路径）**

`src/i18n/keyParity.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import zh from "./locales/zh.json";
import en from "./locales/en.json";

function flattenKeys(obj: Record<string, unknown>, prefix = ""): string[] {
  return Object.entries(obj).flatMap(([key, value]) => {
    const next = prefix ? `${prefix}.${key}` : key;
    if (value && typeof value === "object" && !Array.isArray(value)) {
      return flattenKeys(value as Record<string, unknown>, next);
    }
    return [next];
  });
}

describe("i18n key parity", () => {
  it("keeps zh and en key structures identical", () => {
    expect(flattenKeys(en).sort()).toEqual(flattenKeys(zh).sort());
  });
});
```

并同步更新页面测试的断言来源：替换硬编码文本断言为翻译后可见文本，不允许保留重复硬编码中文兜底。

- [ ] **Step 2: 运行测试，确认失败**

Run:

```bash
pnpm vitest run src/i18n/keyParity.test.ts src/pages/SearchPage.test.tsx src/pages/FavoritesPage.test.tsx src/pages/RecentPlaysPage.test.tsx
```

Expected: FAIL（en key 不完整或页面测试未同步）。

- [ ] **Step 3: 实现 i18n 初始化与页面收敛**

`src/main.tsx` 顶部：

```ts
import "@/i18n";
```

`src/i18n/index.ts`:

```ts
import zh from "./locales/zh.json";
import en from "./locales/en.json";
i18n.use(initReactI18next).init({
  resources: {
    zh: { translation: zh },
    en: { translation: en },
  },
  lng: "zh",
  fallbackLng: "zh",
  interpolation: { escapeValue: false },
});
```

页面/组件改造要求：

1. Sidebar / MiniPlayer / NowPlayingOverlay / Search/Favorites/RecentPlays 全部改用 `useTranslation().t(...)`。
2. 替换后同步修复测试，不允许为“过测”保留同义硬编码中文。

- [ ] **Step 4: 重跑测试，确认通过**

Run:

```bash
pnpm vitest run src/i18n/keyParity.test.ts src/pages/SearchPage.test.tsx src/pages/FavoritesPage.test.tsx src/pages/RecentPlaysPage.test.tsx
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/main.tsx src/i18n/index.ts src/i18n/locales/zh.json src/i18n/locales/en.json src/i18n/keyParity.test.ts src/components/layout/Sidebar.tsx src/components/player/MiniPlayer.tsx src/components/player/NowPlayingOverlay.tsx src/pages/SearchPage.tsx src/pages/FavoritesPage.tsx src/pages/RecentPlaysPage.tsx src/pages/SearchPage.test.tsx src/pages/FavoritesPage.test.tsx src/pages/RecentPlaysPage.test.tsx
git commit -m "refactor: enforce i18n parity and localize v0.7 surfaces"
```

---

### Task 7: Apple Music 视觉验收（结构完整 + 材质正确）

**Files:**
- Modify: `src/styles/globals.css`
- Modify: `src/components/player/NowPlayingOverlay.tsx`
- Modify: `src/components/player/MiniPlayer.tsx`
- Modify: `src/components/player/NowPlayingOverlay.test.tsx`
- Modify: `src/components/player/NowPlayingBackground.test.tsx`

- [ ] **Step 1: 写失败测试（不只测 class，测结构）**

在 `src/components/player/NowPlayingOverlay.test.tsx` 增加断言：

```tsx
expect(screen.getByTestId("now-playing-cover")).toBeInTheDocument();
expect(screen.getByRole("slider", { name: "播放进度" })).toBeInTheDocument();
expect(screen.getByRole("button", { name: "上一首" })).toBeInTheDocument();
expect(screen.getByRole("button", { name: "播放" })).toBeInTheDocument();
expect(screen.getByRole("button", { name: "下一首" })).toBeInTheDocument();
expect(screen.getByRole("slider", { name: "音量" })).toBeInTheDocument();
expect(screen.getByRole("button", { name: "静音" })).toBeInTheDocument();
```

在 `src/components/player/NowPlayingBackground.test.tsx` 增加：

```tsx
const glass = container.querySelector(".np-bg__glass") as HTMLElement;
expect(glass).toBeTruthy();
expect(glass.className).toContain("np-bg__glass");
```

- [ ] **Step 2: 运行测试，确认失败**

Run:

```bash
pnpm vitest run src/components/player/NowPlayingOverlay.test.tsx src/components/player/NowPlayingBackground.test.tsx
```

Expected: FAIL（结构或样式契约未达标）。

- [ ] **Step 3: 实现视觉层与反馈态（含 webkit 材质）**

`src/styles/globals.css`（关键片段）:

```css
.now-playing__play-main {
  width: 64px;
  height: 64px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.18);
  backdrop-filter: blur(18px) saturate(160%);
  -webkit-backdrop-filter: blur(18px) saturate(160%);
}

.np-bg__glass {
  background: rgba(255, 255, 255, 0.12);
  backdrop-filter: blur(24px) saturate(150%);
  -webkit-backdrop-filter: blur(24px) saturate(150%);
}

.now-playing button:hover:not(:disabled) { opacity: 0.92; }
.now-playing button:active:not(:disabled) { transform: scale(0.98); }
.now-playing button:disabled { opacity: 0.45; }
```

要求：确保玻璃层元素存在半透明背景；仅 `backdrop-filter` 不算完成。

- [ ] **Step 4: 重跑测试，确认通过**

Run:

```bash
pnpm vitest run src/components/player/NowPlayingOverlay.test.tsx src/components/player/NowPlayingBackground.test.tsx
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add src/styles/globals.css src/components/player/NowPlayingOverlay.tsx src/components/player/MiniPlayer.tsx src/components/player/NowPlayingOverlay.test.tsx src/components/player/NowPlayingBackground.test.tsx
git commit -m "feat: harden now playing visual structure and glass materials"
```

---

### Task 8: 媒体键边界回归 + 最终门禁

**Files:**
- Modify (optional, only if missing coverage): `src-tauri/src/system/now_playing.rs`

- [ ] **Step 1: 跑系统桥接相关 Rust 测试**

Run:

```bash
CARGO_TARGET_DIR=/tmp/musicplayer-target cargo test --manifest-path src-tauri/Cargo.toml system::now_playing -- --nocapture
```

Expected: PASS。

- [ ] **Step 2: 如有缺口，补最小回归断言**

`src-tauri/src/system/now_playing.rs` 测试片段可补：

```rust
#[test]
fn command_sender_can_be_set_without_panic() {
    let (tx, _rx) = crossbeam_channel::unbounded();
    let mut bridge = NowPlaying::new();
    bridge.set_command_sender(tx);
    bridge.update(&PlayerSnapshot::default());
}
```

- [ ] **Step 3: 前端全量门禁（先测后构建）**

Run:

```bash
pnpm test
pnpm lint
pnpm build
```

Expected: 全部 PASS。

- [ ] **Step 4: Rust 全量门禁**

Run:

```bash
CARGO_TARGET_DIR=/tmp/musicplayer-target cargo fmt --manifest-path src-tauri/Cargo.toml --check
CARGO_TARGET_DIR=/tmp/musicplayer-target cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
CARGO_TARGET_DIR=/tmp/musicplayer-target cargo test --manifest-path src-tauri/Cargo.toml
```

Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/system/now_playing.rs
git commit -m "test: tighten now playing bridge regression coverage"
```

若 Step 2 无文件改动，则本任务不单独提交。

---

## Spec Coverage Self-Review

1. **Now Playing 大屏与视觉硬验收**：Task 2/3/7 覆盖（完整结构、背景三层、玻璃材质、状态反馈、动画存在性）。
2. **MiniPlayer 连续展开体验**：Task 2/7 覆盖（`layoutId` 共享元素与过渡策略）。
3. **快捷键与冲突矩阵**：Task 4 覆盖（Space/Cmd+F/Esc + input/slider/dialog/Web-Tauri 分歧）。
4. **系统媒体键边界**：Task 8 覆盖（不前端双轨，Rust `system::now_playing` 回归）。
5. **主题跟随单源**：Task 1/5 覆盖（store + hook + 无 `matchMedia` 降级）。
6. **i18n 收敛**：Task 6 覆盖（main 引导、文本替换、zh/en key parity）。
7. **不破坏 v0.6 主流程**：Task 8 全量门禁覆盖（先 test 再 build + Rust gate）。

## Placeholder Scan

- 已检查：无 `TBD` / `TODO` / “implement later” 等占位语句。
- 所有任务均给出明确文件、命令、失败/通过预期与最小代码片段。

## Type Consistency Check

- `UIState.theme` 使用 `"system"`，`resolvedTheme` 使用 `"light" | "dark"`，与主题单源设计一致。
- `NowPlayingOverlay` 先执行 Task 2 Step 0 类型核对，再消费现有 `usePlayer()` 字段，不引入新播放器 DTO。
- 快捷键逻辑只调用现有 `player.toggle()` 与路由跳转，不新增 IPC 命令；`Esc` 只有一处全局监听。
- `Cmd+F` 运行时判断使用 `"__TAURI_INTERNALS__" in window`，避免 TS strict 报错。
